import * as p from "tau-prolog";
const s = p.create();
/*

assertz(dom(set_active_filter, saf1)).
assertz(holds(static(filter(saf1), completed))). 
assertz(occurs(saf1, 0)).

assertz(body_satisfied(causal_law7(saf1, completed), 0)).

 */
