// Rule.flip_b(shouldFlip: Bool, b: Bool)
//     if shouldFlip Bool.not(b) else b

// Rule.input_rule(
//     tuples: Set(Sort),
//     negated: Bool, isort: Sort,
//     cb: Set(Sort) -> Set(Set(Sort))) : Set(Sort)
//     Set.filter((attrs)
//         Rule.flip_b(negated, )
//     , tuples)
